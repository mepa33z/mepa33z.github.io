import logging
import sys
from dispatcher import run_dispatch

# Configure logging
logging.basicConfig(
    level = logging.INFO,
    'format = '%(asctime)s -
%(levelname)s - %(message)s'
)

logger = logging.getLogger(__name__)

def main():
    """Main entry point for Reefer Boutique Dispatch System"""
    
    logger.info("Reefer Boutique Dispatch System Starting...")

    try:
        run_dispatch()
    except Exception as e:
        logger.error(f"Dispatch system encountered an error: {e}", exc_info = True)
           sys.exit(1)
    finally:
        logger.info("Reefer Boutique 
Dispatch System Shutting Down...")

if__name__ == "__main__":
   main()



